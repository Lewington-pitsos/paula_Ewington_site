<div class="fixed-top" id="nav-holder">
<nav class="navbar navbar-expand-sm animated fadeOutUp hidden" id="only-navbar">
  <a class="navbar-brand" href="<%= categories_path %>">Home</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link" href="<%=  abouts_path %>">About the Artist</a>
      </li>
      <% if @work && @work.category %>
        <li>
          <%= link_to "Return To #{@work.category.title}", category_path(@work.category.id), class: 'nav-link' %>
        </li>
      <% end %>
      <% if session[:admin] %>
        <li class="nav-item">
          <a class="nav-link" href="<%=  new_category_path %>">Create new Catrgory</a>
        </li>
        <li class="nav-item">
          <%= button_to 'Sign out', "/admins/#{session[:admin]}", method: :delete %>
        </li>
        <% if @category && @category.title %>
          <li>
            <%= link_to "Upload new artwork to #{@category.title}", new_category_works_path(@category.id) %>
          </li>
        <% end %>
      <% end %>
    </ul>
  </div>
</nav>
</div>
