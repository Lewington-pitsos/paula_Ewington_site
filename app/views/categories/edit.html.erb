<h1>Edit <%= @category.title %> info</h1>

<%= form_for @category do |f| %>
  <%= f.label 'Category title:' %>
  <%= f.text_field :title %>
  <br />

  <%= f.label 'Category cover image:' %>
  <%= f.file_field :image %>
  <br />

  <%= f.submit 'Save Changes' %>
<% end %>

<%= link_to 'Cancel', categories_path %>
